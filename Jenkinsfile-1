node {
  stage('SCM') {
    checkout scm
  }
  stage('SonarQube Analysis') {
    def mvn = tool 'Default Maven';
    withSonarQubeEnv() {
      sh "${mvn}/bin/mvn clean verify sonar:master -Dsonar.projectKey=TPqualiteCODE35 -Dsonar.projectName='tpazuremai19'"
    }
  }
}